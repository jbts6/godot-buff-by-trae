# Skills（AI Agent）

本仓库包含若干用于 AI Agent 的 skill（以 Git 子模块形式安装在 `skills/`）。

## 初始化 / 更新

首次拉取仓库后执行：

```bash
git submodule update --init --recursive
```

更新 skill：

```bash
git submodule update --remote --merge
```

## 已安装

- `skills/skill-creator`：https://github.com/wuke3/skill-creator

